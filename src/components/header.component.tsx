import * as React from "react";

class Header extends React.Component {
    render() {
        return(
            <nav>
                <ul>
                    <li>Sign in</li>
                </ul>
            </nav>
        );
    }
}

export default Header;
